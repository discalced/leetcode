package com.leetcode;

public class ValidPalindrome {
	public static void main(String[] args) {
		
	}
	
	public static boolean isPalindrome(String str) {
		if (str.length() == 0) {
			return true;
		}
		for (int i = 0, j = str.length() - 1; i < str.length() && j >= 0; ) {
			if (str.charAt(i) )
		}
	}
}
